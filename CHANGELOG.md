# Changelog

本文件记录可以从冻结文档和当前源码确认的公开里程碑。早期研发曾有大量内部迭代；未保留可验证发布证据的中间编号不会被伪造成 Git 提交。

## 1.2.0

- 将 1.1.9 安装后验证通过的音频热修复正式收回源码与发行包。
- 新增本地 VAD、可选 DeepFilterNet 降噪、Whisper 转写、精确时间戳、文本向量和混合搜索链路。
- 音频任务支持 1～3 路并发、逐视频提交、幂等断点恢复；长期索引不保留中间 WAV。
- 搜索增加“音频（人声转写）”筛选，并显示转写原文、音频时间段、直接命中或语义命中原因。
- 修复低分音频证据误入结果时可能触发空分数组崩溃的问题。
- 保留并验证历史索引切换、回车搜索、人物本地命名、源素材只读和最终完整性闸门。
- 仅发布 Apple Silicon arm64 完整 App；包含脚本和六套运行环境，不包含模型或用户数据。
- 最终 R3 DMG 通过校验、只读挂载、arm64、版本、布局和深度签名检查；真实历史索引搜索验收通过。

详细说明见 [docs/releases/1.2.0.md](docs/releases/1.2.0.md)。

## 1.1.5（本地验证候选，未发布）

- 新增逐阶段 Apple Silicon 硬件、并发与耗时说明；M1 Max 32GB 使用真实任务基线，M5 Max 128GB 和 M3 Ultra 256GB 全部标注为估算。
- 视频抽帧改为每个视频完成后立即验证、写入中央数据库并提交断点，避免阶段末尾才批量落账。
- 运行页补充真实 worker、FFmpeg、成功、跳过、失败、输出文件、输出字节和存储增量。
- ETA 增加最小样本和最短运行时间门槛，样本不足时显示“正在估算”。
- 修复任务停止只终止父进程、子 FFmpeg 仍继续运行的问题，改为可控进程组取消。
- 扫描、去重和重复素材页面排除 AppleDouble `._` 元数据文件，不再把它们当作真实视频或 0 GB 重复素材。
- 重复素材页面改为真实成员并排核对，并提供逐文件定位入口；不自动删除原始素材。
- 人物组增加纯本地名称、标签、合并和撤销能力，不修改中央分析数据库。
- 人物结果按人物组 → 来源素材 → 画面分层展示，减少单个人物数百画面的平铺。
- 搜索文本向量改为批量向量计算，并增加只读数据库身份缓存和视觉向量 mmap 暖缓存。
- 整理模式文案明确区分完整整理、尚未开放的增量整理、补充描述和重建入口。
- 应用版本与版权信息固定为 1.1.5、Horizon-94，并防止首次启动写入 `.pyc` 破坏应用签名。
- 相关短测试共 94 项通过；2.2 TB 从零到 19 阶段的真实验收尚未完成，因此不创建正式 1.1.5 标签或 Release。

详细记录见 [docs/history/1.1.4_TO_1.1.5.md](docs/history/1.1.4_TO_1.1.5.md)。

## 1.1.4

- 搜索过程增加分阶段进度、检查数量和候选结果展示。
- 搜索结果支持流式批次返回。
- 增加常用查询暖缓存与只读仓库缓存。
- 完成 macOS 原生前台、Python 桥接和流水线运行时整合。
- 声明打包图标测试所需的 Pillow 开发依赖，确保干净 CI 环境可复现。
- 拆分搜索页及结果卡片的 SwiftUI 子视图，兼容 GitHub macOS 14 runner 的编译器类型推断限制。
- 升级官方 GitHub Actions 至 v7，移除 Node.js 20 运行时弃用提示。
- 当前代码改为 GPL-3.0-only；保留不可撤销的早期 Apache-2.0 授权历史说明。
- 在应用、DMG 和文档固定 Horizon-94 官方来源与版权信息。
- 增加通用模型根目录、准确相对路径、上游链接和禁止自动下载声明。
- 增加源码构建说明、历史问题整理、发行清单与 DMG 隐私审计。
- 通用运行合同改用项目、环境、模型和工具占位符，不写入维护者电脑路径。
- 修复通用 DMG 构建器对 Homebrew `Python.framework` 的识别，并按所选
  Python 运行时的实际芯片架构生成启动器和原生前台。

## 1.1.3

- 固化完整本地数据库搜索入口。
- 完成应用内只读搜索与运行状态展示。

## 1.0.x

- 完成图片/视频原生界面、任务入口和本地数据库读取。
- 建立应用运行合同、模型合同与构建流程。

## V0.5

- 建立 OCR、Qwen-VL、YOLOE、OpenCLIP 和文本向量的统一证据与混合搜索链路。
- 冻结高价值候选、传播规则与只读搜索合同。

## V0.2–V0.4

- 固化 A9T-v3 图片预览策略。
- 固化 R2J-FIX-C4 视频抽帧策略；C6 仅用于压力测试。
- 完成来源关系、去重、预览和中央数据库收口。

## V0.1

- 建立只读素材扫描、基础清单和可恢复阶段状态。
