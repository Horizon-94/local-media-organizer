# V0.5-2A Closeout / Freeze

## Conclusion

- Stage: V0.5-2A
- Conclusion: PASS
- Scope: V0.5-2A foreground/background supervised execution, V0.1 -> V0.5-2A bounded full-chain, V0.2 dedup/reuse, parallel front-end toolchain, and resource telemetry.

This closeout does not mean all of V0.5 is complete. It does not complete V0.5-2B, model real inference, semantic search, search index construction, or V0.5-3.

## Final RUN_IDs

- Foreground supervised core dedup/reuse PASS: `v05-2A-core-dedup-reuse-20260703-132317`
- Foreground workspace: `/Users/yourname/Documents/AI-Local/test-output/v05-2A-core-dedup-reuse-20260703-132317`
- Background supervised one-shot PASS: `v05-2A-bg-canonical-oneshot-20260703-140319`
- Background workspace: `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319`

`v05-2A-bg-canonical-oneshot-20260703-140319` is the final production-shape evidence for V0.5-2A because it validates the canonical background worker path without foreground fallback.

## Canonical Background Command

The canonical background command is recorded in:

```text
/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/canonical_background_command.json
```

Canonical command summary:

```bash
PYTHONPATH=/Users/yourname/Documents/AI-Local/media-archive-clean \
python3 scripts/v05_2_local_minimal_supervised_runner.py \
  --source /Users/yourname/Documents/MEDIA_ARCHIVE_TEST_SOURCE \
  --workspace <timestamp_workspace> \
  --env-file .env.v05_local_models \
  --execution-mode background_supervised \
  --log-file <log_path> \
  --pid-file <pid_path>
```

Rules frozen by this closeout:

- `background_supervised` is the future app/GUI background worker first-priority entry.
- Foreground fallback is not allowed as a formal background success path.
- `foreground_supervised` is for development, debugging, and manual terminal validation only.
- `background_supervised` is for future GUI / app jobs.
- Foreground and background modes must call the same core runner.

## Passed Capabilities

- V0.1 scan invoked and verified.
- V0.2 build invoked and verified.
- sips parallel execution passed.
- qlmanage / QuickLook multi-format parallel execution passed.
- ffmpeg parallel execution passed.
- R2J-C4 invoked.
- `content_id = sha256:<hash>` is the final content identity.
- Same-content multi-path alias is recorded through `canonical_path`, `all_paths`, and `path_count`.
- Duplicate-derived high-cost artifact reuse passed.
- `image_preview` and `video_frame` artifacts record `generated_once` / `reused_existing`.
- `foreground_supervised` passed.
- `background_supervised` one-shot passed.
- `run_execution_trace.jsonl` passed.
- `resource_telemetry.jsonl` passed.
- `worker_config.json` passed.
- `stage_durations.json` passed.
- `adaptive_scheduling_observations.json` passed.

## Not Included

- V0.5-2B was not entered.
- YOLOE / OCR / visual embedding / Qwen-VL real inference was not started.
- Search index was not built.
- V0.5-3 was not entered.
- 42GB full-source pressure run was not performed.
- 1.3TB project validation was not performed.
- 32TB NAS validation was not performed.
- GUI was not implemented or validated.

## Resource Telemetry

The final background one-shot run generated:

- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/resource_telemetry.jsonl`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/resource_summary.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/worker_config.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/stage_durations.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/adaptive_scheduling_observations.json`

The frozen worker profile is:

- `sips_workers = 8`
- `qlmanage_workers = 8`
- `ffmpeg_workers = 4`
- `model_workers = 1`
- `adaptive_scheduling_enabled = false`

V0.5-2A records telemetry for future dynamic scheduling. It does not change worker counts dynamically and does not complete an adaptive scheduler.

## Dedup / Reuse Contract

The V0.5-2A dedup/reuse contract is frozen as follows:

- `size`, `filename`, and `mtime` are duplicate candidates only.
- SHA-256 is the final same-content identity.
- `content_id = sha256:<hash>`.
- One `content_id` can have multiple `original_paths`.
- Future search results must return `canonical_path`, `all_paths`, and `path_count`.
- The same `content_id` must not generate duplicate `image_preview`, `video_frame`, or future model outputs.
- V0.5-2B must inherit this contract and must not bypass it.

## V0.5-2B Entry Conditions

The prerequisites to allow a later V0.5-2B bounded visual inference task are satisfied:

- foreground/background supervised execution both passed.
- canonical background command is selected.
- dedup/reuse passed.
- resource telemetry exists.
- V0.1 -> V0.5-2A bounded full-chain passed.

V0.5-2B must still obey these constraints:

- It must be based on V0.5-2A artifacts.
- It must not bypass preview, frame, or `content_id`.
- It must not run models repeatedly for duplicate paths.
- Model outputs must attach back to `content_id`, `original_paths`, source path, and timecode.
- V0.5-2B remains bounded visual inference and must not enter V0.5-3.

## No Regression / No Rollback Rules

Future work must not regress these V0.5-2A decisions:

- Do not remove the `content_id` / SHA-256 identity contract.
- Do not restore path-only artifact generation.
- Do not bypass dedup/reuse.
- Do not use foreground fallback as background success.
- Do not delete or ignore `canonical_background_command.json`.
- Do not cite older BLOCKED V0.5-2A runs as the current final status.
- Do not treat V0.4 adapter probe as real inference.

## Report Index

Foreground supervised dedup/reuse evidence:

- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-core-dedup-reuse-20260703-132317/reports/stage_execution_evidence_audit.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-core-dedup-reuse-20260703-132317/reports/dedup_reuse_evidence_v4.json`

Background supervised one-shot evidence:

- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/stage_execution_evidence_audit.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/background_supervised_validation.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/canonical_background_command.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/resource_summary.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/worker_config.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/stage_durations.json`
- `/Users/yourname/Documents/AI-Local/test-output/v05-2A-bg-canonical-oneshot-20260703-140319/reports/adaptive_scheduling_observations.json`

## Final State

V0.5-2A is closed out and frozen as PASS. The next allowed stage is V0.5-2B bounded visual inference. This closeout does not start V0.5-2B, V0.5-3, or search index work.
