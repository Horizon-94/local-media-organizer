# V0.2 Image Preview Freeze

策略名：A9T-v3

证据目录：

`/Users/yourname/Documents/AI-Local/test-output/a9t-v3-generic-image-preview`

判定：PASS

冻结依据：

- 3618 张图片参与判断
- 识别 6 段高置信延时摄影
- 延时摄影总图数 2728
- 延时关键帧 18
- 普通图片 890
- 最终预览 908
- 全部 JPG
- 长边 <= 1280
- 失败 0
- 人工抽查大致正常
- 预览数量减少 2710 张，降幅 74.9%

正式默认规则：

1. 扫描所有图片
2. 所有原始图片仍进入 manifest
3. 按目录 + 扩展名分组
4. 优先使用 file_mtime 判断延时摄影
5. 高置信延时摄影段只抽 first / middle / last
6. 延时关键帧进入 keyframe_pool
7. 延时非关键帧只建档，不生成高成本 preview
8. 非延时图片全量生成 preview
9. 最终全部输出 1280 长边 JPG

后端策略：

- SIPS 直出 JPG：`.jpg` / `.jpeg` / `.png` / `.tif` / `.tiff` / `.dng` / `.heic` / `.heif` / `.hif`
- system_preview 后转 JPG：`.arw` / `.cr2` / `.cr3` / `.nef` / `.nrw` / `.rw2` / `.raf` / `.orf`

system_preview 流程：

qlmanage 临时图 -> sips 转 1280 JPG -> 删除临时图

并发冻结：

- SSD 默认：SIPS workers = 8
- SSD 默认：system_preview workers = 8
- 机械硬盘不照搬，后续单独低并发测试

## 已迁移实现

A9T-v3 图片预览策略已迁移到 `media-archive-clean`。

当前 clean 项目实现仅覆盖图片预览子项：按目录 + 扩展名分组，优先使用 file_mtime 判断高置信延时摄影，高置信延时摄影只抽 first / middle / last，普通图片全量生成 preview。

本记录不改变原冻结证据，不证明 V0.2 完整完成，不替代 R2J-FIX-C4 视频抽帧、去重、异常处理或真实 3618 图片验收。
