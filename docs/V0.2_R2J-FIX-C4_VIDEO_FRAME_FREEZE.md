# V0.2 Video Frame Freeze

策略名：R2J-FIX-C4

证据目录：

`/Users/yourname/Documents/AI-Local/test-output/v02-r2j-fix-iphone-13gb-hevc-C4-videotoolbox-concurrency4`

判定：PASS

默认参数：

- video_frame_strategy: R2J-FIX-C4
- decode_mode: videotoolbox
- ffmpeg_extra_args: ["-hwaccel", "videotoolbox"]
- default_concurrency: 4
- high_performance_concurrency: 6
- C6 不作为默认
- sampling_offset_ms: 1000
- sampling_interval_ms: 2000
- max_edge_px: 1280
- final_output_format: jpg
- fallback_attempted: false
- showinfo_used: false

冻结依据：

- video_files_total: 34
- total_produced_frame_count: 672
- total_valid_jpg1280_count: 672
- total_invalid_jpg_count: 0
- frame_extract_status_counts: {"success": 34}
- fallback_attempted_count: 0
- showinfo_used_count: 0
- total_task_wall: 189.843 秒

正式默认规则：

1. 原始素材只读
2. 视频递归扫描
3. 每个视频一次 FFmpeg
4. HEVC/H.265 优先使用 -hwaccel videotoolbox
5. 默认 concurrency = 4
6. sampling_offset_ms = 1000
7. sampling_interval_ms = 2000
8. 输出 JPG
9. 长边 <= 1280
10. 不使用 showinfo
11. 不为了补理论帧数启动 fallback
12. expected_frame_count_estimate 只做参考记录
13. produced_frame_count 是事实
14. 边界少 1 帧允许记录 tolerated_boundary_difference
15. 每帧 manifest 后续必须记录 source_video_path、source_video_relative_path、frame_file、frame_relative_path、frame_index、estimated_frame_time_ms、sampling_offset_ms、sampling_interval_ms、decode_mode、concurrency、video_frame_strategy

## 已迁移实现

R2J-FIX-C4 视频抽帧策略已迁移到 `media-archive-clean`。

当前 clean 项目实现仅覆盖视频抽帧子项：递归扫描视频，默认 decode_mode 为 videotoolbox，默认 concurrency 为 4，记录 high_performance_concurrency 为 6，sampling_offset_ms 为 1000，sampling_interval_ms 为 2000，输出 JPG，长边约束为 1280，不使用 showinfo，不启动 fallback 补帧。

本记录不改变原冻结证据，不证明 V0.2 完整完成，不替代 A9T-v3 图片预览、去重、异常处理、统一 manifest 或真实 13GB 视频验收。
